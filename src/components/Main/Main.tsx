import React from "react";
import ImageCarousel from "../ImageCarousel/ImageCarousel";

const Main: React.FC = () => {
    return (
        <main>
            <ImageCarousel />
        </main>
    );
}

export default Main;